# Android / Gradle
*.iml
.gradle/
build/
captures/
.externalNativeBuild/
.cxx/
local.properties

# Android Studio / IntelliJ
.idea/
*.hprof

# Kotlin build daemon / session cache
.kotlin/

# Keystore / secrets (do not commit signing keys)
*.jks
*.keystore
keystore.properties

# OS
.DS_Store
Thumbs.db

# Python (test harness)
tools/__pycache__/
tools/venv/
*.pyc

# Local test captures / logs (e.g. ffplay/ffprobe stream dumps)
video_log
*.log

# Reverse-engineering scratch (decompiled sources, extracted apk contents)
/decompiled/
/apk-extracted/

# Original app's copyrighted audio, extracted for local A/B review only.
# Must NEVER reach a remote — the shipped app uses original synthesized audio.
/original-sounds/

# The original firmware APK is large (54 MB) and is a binary reference artifact.
# It is intentionally NOT committed. Keep a local copy; store in Git LFS or a
# release asset if you need it in the repo.
*.apk
